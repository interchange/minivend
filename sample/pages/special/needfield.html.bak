<html> <head>
<title>Required field(s) Missing</title>
</head>

[body 2]
<h1>We need some more information!</h1>

We need to have some fields entered to be able
to process your order.  Please go back and fill out 
the information.
<P>
Problem: 
<P>
[perl]
REDO: {
	my $msg = q%[subject]%;
	my(@msg);
	my(%msg);
	sub redo_message {
		my($param,$word) = @_;
		return "$param: $word<BR>" unless $param =~ /mv_contingent/;
		return "You need to select your method of payment.<BR>";
	}
	$msg = s/^\s+//;
	@msg = split /(\w+):\s+/, $msg;
	%msg = @msg;
	foreach $key (keys %msg) {
		$msg .= redo_message($key, $msg{$key});
	}
	$msg;
}
[/perl]
<P>
[finish-order]
<P>
[[buttonbar 1]]
</body></html>
