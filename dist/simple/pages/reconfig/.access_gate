upload: [if session username eq '__MVC_SUPERUSER__']Yes[else]No[/else][/if]
do_upload: [if session username eq '__MVC_SUPERUSER__']Yes[else]No[/else][/if]
do_download: [if session username eq '__MVC_SUPERUSER__']Yes[else]No[/else][/if]
reconfig: [if session username eq '__MVC_SUPERUSER__']Yes[else]No[/else][/if]
*: 0
